// import { Reducer } from "redux";
// import { AppState, initialState } from "./counterReducer";
// import { CounterActionTypes, TodosActionTypes } from "../actions";


// const counterReducer: Reducer<AppState> = (state = initialState, action) => {
//     switch (action.type) {
//         case TodosActionTypes.AddTodo   :
//             return { ...state , todos: [...state.todos] };
//         case TodosActionTypes.RemoveTodo:
//             return { ...state, count: state.count - 1 };
//         default:
//             return state;
//     }
// }

// export default counterReducer;

export {}